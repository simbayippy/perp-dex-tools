# Core Trading Client Dependencies
python-dotenv>=1.0.0
pytz>=2025.2
pydantic>=1.8.0
pycryptodome>=3.15.0
ecdsa>=0.17.0
requests>=2.32.0

# Exchange Client Library
# IMPORTANT: Install the shared exchange library with all exchange SDKs:
#   pip install -e './exchange_clients[all]'
#
# Or install only specific exchanges you need:
#   pip install -e './exchange_clients[lighter,grvt,edgex,backpack]'
#
# Available exchanges: lighter, grvt, edgex, backpack, paradex, aster
